name: macOS CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
        
    - name: Build macOS app
      run: xcodebuild -workspace ChatApp.xcodeproj -scheme ChatApp -configuration Release clean build
